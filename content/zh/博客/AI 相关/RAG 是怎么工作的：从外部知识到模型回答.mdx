---
title: RAG 是怎么工作的：从外部知识到模型回答
slug: rag-how-it-works
date: 2026-07-15
priority: 5
allowCopy: true
description: 从文档解析、切块和索引，到混合检索、重排、上下文组装与引用，系统解释 RAG 如何把外部知识转化为可回查的模型回答。
---

# RAG 是怎么工作的：从外部知识到模型回答

公司在 7 月发布了一份新的差旅报销政策。文件里写着各城市的住宿标准、交通费用、审批条件和生效日期，其中上海地区的酒店上限改成了每晚 600 元。

这份政策刚刚发布，基础模型训练时没有见过。员工在内部助手里询问：

> 上海出差住酒店，每晚最多可以报销多少钱？

让模型凭自己的知识回答，它会说不知道，也可能根据常见差旅标准猜一个数字。公司当然不能接受后者。开发者需要让模型在回答前找到最新政策，读到上海对应的条款，再把金额和生效条件说清楚。

RAG 处理的是这段连接。它的全称是 Retrieval-Augmented Generation，通常翻译成检索增强生成。外部文件不会因为进入知识库，就被重新训练进模型参数。每当用户提出问题，系统先去外部知识中寻找相关材料，再把找到的内容放进本次请求的上下文，最后让模型基于这些材料生成回答。

```mermaid
graph TB
  subgraph Source[外部资料进入系统]
    direction LR
    A1[差旅政策 PDF] --> A2[解析正文与表格] --> A3[清理页眉和噪声]
    A3 --> A4[切分内容片段] --> A5[补充来源和日期] --> A6[写入检索索引]
  end
  subgraph Question[用户发起查询]
    direction LR
    B1[上海住宿上限是多少] --> B2[理解查询意图] --> B3[识别地区和时间条件]
    B3 --> B4[生成检索查询]
  end
  subgraph Retrieve[寻找候选材料]
    direction LR
    C1[关键词召回] ~~~ C2[向量召回] ~~~ C3[Metadata 过滤]
    C3 ~~~ C4[合并候选] ~~~ C5[重新排序]
  end
  subgraph Context[组织模型上下文]
    direction LR
    D1[删除重复片段] --> D2[检查版本和权限] --> D3[保留关键条款]
    D3 --> D4[绑定文件与页码] --> D5[控制上下文长度]
  end
  subgraph Answer[生成可以回查的回答]
    direction LR
    E1[用户问题] ~~~ E2[系统指令] ~~~ E3[检索材料]
    E3 ~~~ E4[模型生成回答] ~~~ E5[返回金额和引用]
  end
  Source --> Question
  Question --> Retrieve
  Retrieve --> Context
  Context --> Answer
  classDef source fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef question fill:#FFF8E1,stroke:#FFD54F,color:#5D4037
  classDef retrieve fill:#E0F2F1,stroke:#4DB6AC,color:#004D40
  classDef context fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  classDef answer fill:#E8F5E9,stroke:#81C784,color:#1B5E20
  class A1,A2,A3,A4,A5,A6 source
  class B1,B2,B3,B4 question
  class C1,C2,C3,C4,C5 retrieve
  class D1,D2,D3,D4,D5 context
  class E1,E2,E3,E4,E5 answer
```

这条链路实际上由两段工作组成。文档入库发生在用户提问之前，负责把原始文件变成可以查询的索引；在线查询发生在用户提问之后，负责召回材料、整理上下文和生成答案。

## 外部知识没有写进模型参数

Patrick Lewis 等人在 2020 年发表的论文《Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks》，把模型使用的知识分成了两类。

一类知识保存在模型参数中。模型通过训练见过大量文本，语言规律、常见事实和概念关系被压进参数。它可以流畅地回答一般问题，但参数中的知识有更新时间，想改掉一条错误事实，通常也不能像修改数据库记录那样直接。

另一类知识放在模型之外。原始论文使用 Wikipedia 的稠密向量索引作为外部记忆，检索器根据输入找到相关段落，生成模型再结合这些段落输出答案。外部知识可以更新，也能为答案保留来源。论文里的实验系统还不是今天常见的企业知识库产品，但“生成以前先访问外部知识”这条思路保留了下来。

现在开发者说 RAG，范围已经宽得多。资料可以来自 PDF、Word、数据库、网页、工单或者代码仓库；检索可以使用关键词、向量、过滤条件和知识图谱；模型也未必与检索器联合训练。它们共同保留的关系是：知识存放在模型外部，系统在请求发生时按需取回一部分，生成模型只处理当前取回的内容。

```mermaid
graph TB
  subgraph Original[2020 年论文中的 RAG]
    direction LR
    A1[输入问题] --> A2[稠密检索器] --> A3[Wikipedia 向量索引]
    A3 --> A4[取回相关段落] --> A5[Seq2Seq 生成模型] --> A6[知识密集型答案]
  end
  subgraph Memory[两种知识来源]
    direction LR
    B1[参数化知识] ~~~ B2[模型训练时写入] ~~~ B3[更新成本较高]
    B3 ~~~ B4[非参数化知识] ~~~ B5[存放在外部索引] ~~~ B6[可以单独更新]
  end
  subgraph Modern[现代工程中的 RAG]
    direction LR
    C1[企业文件和业务数据] --> C2[解析切块与 Metadata] --> C3[关键词和向量索引]
    C3 --> C4[召回与重排] --> C5[上下文组装] --> C6[大模型生成]
  end
  subgraph Shared[持续保留下来的核心关系]
    direction LR
    D1[知识位于模型外部] ~~~ D2[查询时按需检索] ~~~ D3[材料进入当前上下文]
    D3 ~~~ D4[回答可以绑定来源] ~~~ D5[知识更新不必重训模型]
  end
  Original --> Memory
  Memory --> Modern
  Modern --> Shared
  classDef original fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef memory fill:#FFF8E1,stroke:#FFD54F,color:#5D4037
  classDef modern fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  classDef shared fill:#E8F5E9,stroke:#81C784,color:#1B5E20
  class A1,A2,A3,A4,A5,A6 original
  class B1,B2,B3,B4,B5,B6 memory
  class C1,C2,C3,C4,C5,C6 modern
  class D1,D2,D3,D4,D5 shared
```

因此，RAG 也不会让模型从此“记住”报销标准。系统下一次回答同类问题时，仍然需要检索。如果文件从知识库删除，或者权限过滤不允许当前用户访问，模型就不应该继续引用这条规定。外部知识的可更新、可控制和可追溯，恰好建立在它没有被写死进模型参数这件事上。

## 文档先被加工成检索索引

一份 PDF 不能原封不动地塞进向量数据库。入库链路要先把面向人阅读的文件，转换成面向检索的内容单元。

第一步是解析。系统需要从 PDF、Word、网页或扫描件中提取文字，识别标题、章节、列表和表格。差旅政策里的城市与金额很可能放在表格中，如果解析器只取到了表头，没有把每一行的城市和标准对应起来，后面使用再好的 Embedding 也无济于事。

解析以后还要清理。页眉、页脚、页码和重复导航会污染索引；扫描 OCR 可能把 `600` 识别成 `800`；附件和正文之间的关系也可能在转换中丢失。RAG 的知识质量从这一层就已经开始分化。

接下来是切块。文档通常比模型一次需要的证据长得多，搜索引擎也更适合返回一段具体条款，而不是整本员工手册。系统会把文档分成若干 Chunk，再为每个 Chunk 记录文件名、章节、页码、发布日期、部门、权限和版本等 Metadata。

最后才是生成 Embedding 和建立索引。Embedding 把文本转换成能够参与相似度比较的数字表示，索引则让系统可以在大量片段中快速找到候选。OpenAI 当前的 Vector Store 会在文件加入时自动执行切块、Embedding 和索引，默认切块大小是 800 Token、相邻片段重叠 400 Token；自建 RAG 时，这几步需要由应用或检索平台完成。

```mermaid
graph TB
  subgraph Files[原始资料]
    direction LR
    A1[PDF 政策] ~~~ A2[Word 制度] ~~~ A3[网页公告] ~~~ A4[扫描件] ~~~ A5[数据库记录]
  end
  subgraph Parse[解析与结构恢复]
    direction LR
    B1[提取正文] --> B2[识别标题层级] --> B3[恢复表格行列]
    B3 --> B4[识别列表和附件] --> B5[保留页码位置] --> B6[记录解析异常]
  end
  subgraph Clean[清理与规范化]
    direction LR
    C1[删除重复页眉] ~~~ C2[修正乱码和 OCR] ~~~ C3[统一日期和数字]
    C3 ~~~ C4[删除无意义空白] ~~~ C5[保留原始文件引用]
  end
  subgraph Chunk[切块与补充信息]
    direction LR
    D1[按章节或长度切分] --> D2[设置相邻重叠] --> D3[绑定标题路径]
    D3 --> D4[添加文件与页码] --> D5[添加时间和部门] --> D6[添加权限标签]
  end
  subgraph Index[建立可查询索引]
    direction LR
    E1[生成文本 Embedding] ~~~ E2[保存原始 Chunk] ~~~ E3[建立向量索引]
    E3 ~~~ E4[建立关键词索引] ~~~ E5[保存 Metadata] ~~~ E6[记录索引版本]
  end
  Files --> Parse
  Parse --> Clean
  Clean --> Chunk
  Chunk --> Index
  classDef files fill:#ECEFF1,stroke:#90A4AE,color:#263238
  classDef parse fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef clean fill:#E0F2F1,stroke:#4DB6AC,color:#004D40
  classDef chunk fill:#FFF8E1,stroke:#FFD54F,color:#5D4037
  classDef index fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  class A1,A2,A3,A4,A5 files
  class B1,B2,B3,B4,B5,B6 parse
  class C1,C2,C3,C4,C5 clean
  class D1,D2,D3,D4,D5,D6 chunk
  class E1,E2,E3,E4,E5,E6 index
```

切块，块太大，一个结果里会混入许多不相关条款，占用上下文；块太小，金额可能和它所属的地区、人员类型或生效日期分开。固定长度切分实现简单，却可能从表格或句子中间落刀。按章节切分能保留结构，但有些章节又会长到无法直接使用。

相邻重叠可以减少边缘信息被切断的概率，代价是索引内容增加、召回结果更容易重复。另一种方法是给 Chunk 补充文档背景。Anthropic 的 Contextual Retrieval 会让模型根据整篇文档，为每个片段生成一小段说明，再把说明放到片段前面建立向量和 BM25 索引。

例如原始片段只有：

> 住宿标准调整为每晚 600 元，自 7 月 1 日起执行。

补充背景以后可以变成：

> 本片段来自 2026 年正式员工差旅政策的上海地区住宿章节。住宿标准调整为每晚 600 元，自 7 月 1 日起执行。

用户搜索“上海酒店报销上限”时，后者更容易被正确召回。这里增加的内容必须来自原文上下文，不能顺手编造文件里没有的条件。

```mermaid
graph TB
  subgraph Document[原始政策中的完整关系]
    direction LR
    A1[上海地区住宿标准] --> A2[适用正式员工] --> A3[每晚上限 600 元]
    A3 --> A4[2026 年 7 月生效] --> A5[超标需要额外审批]
  end
  subgraph Large[整篇或超大切块]
    direction LR
    B1[保留完整背景] ~~~ B2[同时混入交通和餐补] ~~~ B3[召回定位不够精确]
    B3 ~~~ B4[占用较多上下文]
  end
  subgraph Small[过小的固定切块]
    direction LR
    C1[只剩每晚 600 元] ~~~ C2[地区信息被切走] ~~~ C3[生效日期被切走]
    C3 ~~~ C4[容易被错误套用]
  end
  subgraph Structured[按结构切块]
    direction LR
    D1[沿章节和表格行切分] ~~~ D2[保留标题路径] ~~~ D3[相邻片段适度重叠]
    D3 ~~~ D4[绑定页码与版本]
  end
  subgraph Contextual[上下文化切块]
    direction LR
    E1[读取整篇文档背景] --> E2[生成片段说明] --> E3[说明与原片段拼接]
    E3 --> E4[建立向量索引] --> E5[建立 BM25 索引] --> E6[评测召回改善]
  end
  Document --> Large
  Large --> Small
  Small --> Structured
  Structured --> Contextual
  classDef document fill:#E8F5E9,stroke:#81C784,color:#1B5E20
  classDef large fill:#FFF3E0,stroke:#FFB74D,color:#E65100
  classDef small fill:#FFEBEE,stroke:#E57373,color:#B71C1C
  classDef structured fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef contextual fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  class A1,A2,A3,A4,A5 document
  class B1,B2,B3,B4 large
  class C1,C2,C3,C4 small
  class D1,D2,D3,D4 structured
  class E1,E2,E3,E4,E5,E6 contextual
```

没有一种切块策略能脱离文档类型直接成为标准答案。产品手册适合沿标题和段落切分，合同要尽量保住条款结构，表格需要让表头跟着数据行，代码则要考虑函数和类的范围。切块效果要回到真实问题上检查，而不是只看每段长度是否整齐。

## 用户问题经过多路检索找到候选

索引准备完成以后，用户问题才进入在线查询链路。

“上海出差住酒店，每晚最多可以报销多少钱”已经比较具体，但真实输入经常含糊得多。用户可能只写“上海住宿标准”，也可能在长对话后追问“那 7 月以后呢”。系统需要结合当前问题和必要的对话信息，整理出适合搜索的查询。

查询改写不是替用户添加条件，而是把对检索有用的信息说清楚。OpenAI Retrieval API 提供可选的自动 Query Rewrite；更复杂的应用也可能自己提取实体、缩写、时间范围和同义表达。与此同时，系统可以根据用户身份和问题内容生成 Metadata Filter，只在有效日期、指定部门和当前用户有权访问的文件中搜索。

随后会发生一项或多项检索。关键词检索查找明确出现的词语，向量检索把问题生成 Embedding，再寻找语义表示相近的片段。两路结果可以通过混合检索合并，形成一个覆盖较广的候选集合。

```mermaid
graph TB
  subgraph Input[查询输入]
    direction LR
    A1[当前用户问题] ~~~ A2[必要的对话信息] ~~~ A3[用户部门和权限] ~~~ A4[当前时间]
  end
  subgraph Understand[查询整理]
    direction LR
    B1[识别查询目标] --> B2[补全省略指代] --> B3[提取上海和住宿]
    B3 --> B4[识别 7 月时间条件] --> B5[生成检索表达] --> B6[保留原始问题]
  end
  subgraph Filter[检索前过滤]
    direction LR
    C1[限定差旅政策] ~~~ C2[限定有效版本] ~~~ C3[限定可访问部门]
    C3 ~~~ C4[排除草稿文件] ~~~ C5[排除过期制度]
  end
  subgraph Search[并行召回]
    direction LR
    D1[关键词搜索金额和地名] ~~~ D2[向量搜索语义相近片段] ~~~ D3[可选结构化字段查询]
    D3 ~~~ D4[各路返回 Top N] ~~~ D5[保留来源和分数]
  end
  subgraph Candidates[候选结果集合]
    direction LR
    E1[上海正式员工标准] ~~~ E2[上海实习生标准] ~~~ E3[旧版 500 元标准]
    E3 ~~~ E4[外地交通补贴] ~~~ E5[超标审批说明] ~~~ E6[候选等待重排]
  end
  Input --> Understand
  Understand --> Filter
  Filter --> Search
  Search --> Candidates
  classDef input fill:#ECEFF1,stroke:#90A4AE,color:#263238
  classDef understand fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef filter fill:#FFF8E1,stroke:#FFD54F,color:#5D4037
  classDef search fill:#E0F2F1,stroke:#4DB6AC,color:#004D40
  classDef candidates fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  class A1,A2,A3,A4 input
  class B1,B2,B3,B4,B5,B6 understand
  class C1,C2,C3,C4,C5 filter
  class D1,D2,D3,D4,D5 search
  class E1,E2,E3,E4,E5,E6 candidates
```

Embedding 容易被描述成某种会理解一切的语义魔法，实际职责窄得多。它把文本映射成一组数字，让意思接近的文本在表示空间中更容易靠近。查询写“出差酒店”，文档写“差旅住宿”，即使没有完全相同的词，向量搜索也有机会找到它。

这项能力不负责判断片段是否为最新版本，也不负责从条款中生成最终答案。产品编号、金额、法条编号、人名和公司内部缩写，有时更依赖精确匹配。用户搜索 `BX-2026-07`，关键词检索往往比单纯的语义相似更直接。

因此，关键词与向量不是非此即彼。Azure AI Search 对 Hybrid Search 的说明就是让两种查询并行执行，再把结果合并排序。OpenAI Retrieval API 也允许调整语义向量匹配和稀疏关键词匹配在混合检索中的权重。具体比例仍要根据业务问题评测。

```mermaid
graph TB
  subgraph Query[同一个用户问题]
    direction LR
    A1[上海出差酒店上限] ~~~ A2[包含地区] ~~~ A3[包含业务概念] ~~~ A4[没有原文完整措辞]
  end
  subgraph Keyword[关键词检索]
    direction LR
    B1[匹配上海和酒店] --> B2[擅长编号金额和专有名词] --> B3[结果容易解释]
    B3 --> B4[可能漏掉差旅住宿等同义表达]
  end
  subgraph Vector[向量检索]
    direction LR
    C1[生成查询 Embedding] --> C2[寻找语义相近向量] --> C3[召回差旅住宿条款]
    C3 --> C4[可能混入语义相关但条件不同的内容]
  end
  subgraph Hybrid[混合检索]
    direction LR
    D1[分别取得两路候选] --> D2[归一化或融合排名] --> D3[保留精确命中]
    D3 --> D4[补充语义命中] --> D5[扩大相关资料覆盖] --> D6[交给 Reranker]
  end
  subgraph Choice[适用判断]
    direction LR
    E1[编号金额优先关键词] ~~~ E2[自然语言优先语义召回] ~~~ E3[企业问答常用混合检索]
    E3 ~~~ E4[最终选择依赖评测集]
  end
  Query --> Keyword
  Keyword --> Vector
  Vector --> Hybrid
  Hybrid --> Choice
  classDef query fill:#ECEFF1,stroke:#90A4AE,color:#263238
  classDef keyword fill:#FFF8E1,stroke:#FFD54F,color:#5D4037
  classDef vector fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef hybrid fill:#E0F2F1,stroke:#4DB6AC,color:#004D40
  classDef choice fill:#E8F5E9,stroke:#81C784,color:#1B5E20
  class A1,A2,A3,A4 query
  class B1,B2,B3,B4 keyword
  class C1,C2,C3,C4 vector
  class D1,D2,D3,D4,D5,D6 hybrid
  class E1,E2,E3,E4 choice
```

第一次搜索通常倾向于多找一些。相关材料一旦没有进入候选集合，后面的模型和 Reranker 都无法把它救回来；候选稍多，还可以在下一阶段继续筛选。这个取舍对应检索中的召回与精度：前段尽量别漏，后段再减少噪声。

## 候选材料要经过重排和组装

向量相似度高，不等于片段最能回答当前问题。

候选集合里可能同时出现上海正式员工标准、实习生标准、去年的旧版本和超标审批流程。它们都和“上海住宿报销”有关，真正能够直接回答当前问题的却只有一部分。Reranker 会把用户问题和每个候选放在一起判断相关性，再重新排列顺序。

Cohere 的 Rerank 文档把它放在关键词或向量搜索之后：企业可以保留现有第一阶段检索，再把候选送入第二阶段重排。第一阶段适合从大规模索引快速取回几十甚至上百个候选，第二阶段计算更细，但只处理这批有限内容。Anthropic 的 Contextual Retrieval 实验也是先取回候选，再通过 Reranker 把最终送给模型的片段缩小。

重排之后还不能直接把结果连起来。上下文组装需要删除重复 Chunk，检查文件权限与版本，处理互相冲突的规定，并在 Token 预算内选择足够的证据。每个片段还要保留文件名、页码、标题路径或 URL，后面生成的主张才有引用入口。

```mermaid
graph TB
  subgraph Recall[第一阶段扩大召回]
    direction LR
    A1[关键词候选] ~~~ A2[向量候选] ~~~ A3[结构化查询候选]
    A3 ~~~ A4[合并几十个结果] ~~~ A5[保留来源和初始分数]
  end
  subgraph Rerank[第二阶段重新排序]
    direction LR
    B1[同时阅读问题与候选] --> B2[判断能否直接回答] --> B3[降低旧版和旁支内容]
    B3 --> B4[提高完整条款] --> B5[选择最终 Top K] --> B6[保留重排分数]
  end
  subgraph Validate[进入上下文前检查]
    direction LR
    C1[核对访问权限] ~~~ C2[核对生效日期] ~~~ C3[识别新旧版本冲突]
    C3 ~~~ C4[删除重复片段] ~~~ C5[标记证据不足]
  end
  subgraph Assemble[组装模型上下文]
    direction LR
    D1[按问题组织片段] --> D2[保留必要上下文] --> D3[绑定来源编号]
    D3 --> D4[控制 Token 总量] --> D5[隔离资料与指令] --> D6[形成 Evidence Pack]
  end
  subgraph Final[最终交给模型的材料]
    direction LR
    E1[上海 600 元条款] ~~~ E2[7 月生效条件] ~~~ E3[超标审批规则]
    E3 ~~~ E4[文件名和页码] ~~~ E5[未采用旧版条款]
  end
  Recall --> Rerank
  Rerank --> Validate
  Validate --> Assemble
  Assemble --> Final
  classDef recall fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef rerank fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  classDef validate fill:#FFF8E1,stroke:#FFD54F,color:#5D4037
  classDef assemble fill:#E0F2F1,stroke:#4DB6AC,color:#004D40
  classDef final fill:#E8F5E9,stroke:#81C784,color:#1B5E20
  class A1,A2,A3,A4,A5 recall
  class B1,B2,B3,B4,B5,B6 rerank
  class C1,C2,C3,C4,C5 validate
  class D1,D2,D3,D4,D5,D6 assemble
  class E1,E2,E3,E4,E5 final
```

Top K 也不是越大越好。增加片段可以减少漏掉条件的概率，同时会增加延迟、Token 成本和模型被无关材料干扰的风险。OpenAI File Search 允许限制返回结果数量，官方文档也明确提醒：数量下降可以减少 Token 和延迟，但可能损失回答质量。这个参数没有脱离业务问题的理想值。

上下文还要处理一种麻烦情况：两份文件都像正式规定，却给出不同金额。系统不能因为某个片段相似度高一点，就擅自宣布它正确。有效日期、文件状态和发布部门应该在检索或组装阶段参与判断；仍然无法消除的冲突，要一并交给模型，并要求回答明确说明分歧。

## 模型根据检索材料组织回答

完成检索和上下文组装后，生成模型才真正登场。

它收到的不只有用户问题。系统指令会规定回答方式，例如只能依据提供的资料、证据不足时不能猜测、主要事实需要标注来源。Evidence Pack 里保存被选中的条款及其文件信息。应用还可能附带用户身份、输出格式和安全要求。

模型负责综合这些输入，生成一段自然语言回答：

> 根据《2026 年员工差旅管理办法》第 4 页，上海地区正式员工的住宿标准自 7 月 1 日起为每晚不超过 600 元。超出标准需要提前申请额外审批。

这句话里其实包含三个可以分别核查的主张：适用对象是正式员工，金额是 600 元，生效时间是 7 月 1 日。引用应当支持附近的具体主张，而不是只在段末放一个主题相近的文件名。

引用能力从检索阶段就开始建立。如果 Chunk 没有保留文件和页码，Writer 写完以后再去猜来源，找到的页面可能与句子主题相似，却没有真正包含相应规定。OpenAI Retrieval API 的搜索结果会返回 Chunk、分数、文件和 Attributes，这些信息正是生成引用和回查入口的基础。

```mermaid
graph TB
  subgraph Evidence[可追溯的检索证据]
    direction LR
    A1[政策文件 ID] --> A2[第 4 页住宿章节] --> A3[上海 600 元条款]
    A3 --> A4[7 月 1 日生效条款] --> A5[超标审批条款] --> A6[保留原文位置]
  end
  subgraph Prompt[模型收到的完整输入]
    direction LR
    B1[系统回答规则] ~~~ B2[用户原始问题] ~~~ B3[用户权限信息]
    B3 ~~~ B4[带编号的证据片段] ~~~ B5[输出和引用要求]
  end
  subgraph Generate[生成时进行的工作]
    direction LR
    C1[识别问题所需事实] --> C2[在材料中定位答案] --> C3[综合多个限定条件]
    C3 --> C4[避免补写无来源信息] --> C5[组织自然语言] --> C6[插入来源编号]
  end
  subgraph Verify[交付前检查]
    direction LR
    D1[金额是否来自原文] ~~~ D2[适用对象是否正确] ~~~ D3[时间条件是否遗漏]
    D3 ~~~ D4[引用是否支持主张] ~~~ D5[冲突是否得到说明]
  end
  subgraph Output[用户最终看到]
    direction LR
    E1[直接回答 600 元] ~~~ E2[说明正式员工条件] ~~~ E3[说明生效日期]
    E3 ~~~ E4[补充超标审批] ~~~ E5[提供文件和页码]
  end
  Evidence --> Prompt
  Prompt --> Generate
  Generate --> Verify
  Verify --> Output
  classDef evidence fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef prompt fill:#FFF8E1,stroke:#FFD54F,color:#5D4037
  classDef generate fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  classDef verify fill:#E0F2F1,stroke:#4DB6AC,color:#004D40
  classDef output fill:#E8F5E9,stroke:#81C784,color:#1B5E20
  class A1,A2,A3,A4,A5,A6 evidence
  class B1,B2,B3,B4,B5 prompt
  class C1,C2,C3,C4,C5,C6 generate
  class D1,D2,D3,D4,D5 verify
  class E1,E2,E3,E4,E5 output
```

RAG 能给模型材料，不会自动强迫模型忠实。模型仍然可能忽略限定条件、混合两份冲突资料，或者调用参数记忆补上一句看似合理的话。Prompt、引用检查和交付前验证仍然有用。高风险的财务、法律或医疗内容，还需要更严格的规则与人工复核。

同样，找到了正确片段也不代表一定能回答。检索结果可能只是提到“上海住宿标准已调整”，却没有具体金额。可靠系统应该暴露证据缺口，而不是把“找到了相关内容”误判成“找到了答案”。

## 回答错误要沿着链路逐层检查

员工最后得到错误金额时，最显眼的是模型回答错了，问题却可能发生在前面任何一层。

知识库可能根本没有上传 2026 年政策，或者索引任务仍在处理中。PDF 解析可能丢掉表格中的一列。切块可能把“上海”与“600 元”分开。查询过滤可能错误地选中了实习生制度。向量召回找到了大量酒店相关内容，却漏掉精确金额。Reranker 可能把旧版政策排在最前面。上下文组装也可能同时放入 500 元和 600 元，却没有标注各自的生效日期。

直到前面的材料都正确，才轮到检查模型有没有忠实回答。只修改生成 Prompt，会让某些样本碰巧变好，却修复不了损坏的 PDF 表格和错误的版本过滤。

```mermaid
graph TB
  subgraph SourceCheck[检查知识源]
    direction LR
    A1[最新文件是否存在] ~~~ A2[版本状态是否正确] ~~~ A3[用户是否有访问权限]
    A3 ~~~ A4[旧文件是否已经停用] ~~~ A5[索引任务是否完成]
  end
  subgraph ParseCheck[检查解析和切块]
    direction LR
    B1[正文是否完整] ~~~ B2[表格金额是否保留] ~~~ B3[标题与条款是否绑定]
    B3 ~~~ B4[Chunk 是否丢失条件] ~~~ B5[Metadata 是否准确]
  end
  subgraph RetrieveCheck[检查召回]
    direction LR
    C1[原始查询是什么] --> C2[查询是否被错误改写] --> C3[过滤条件是否过严]
    C3 --> C4[正确片段是否进入 Top N] --> C5[关键词和向量各自命中什么]
  end
  subgraph RankCheck[检查重排和上下文]
    direction LR
    D1[正确片段排名] ~~~ D2[旧版片段排名] ~~~ D3[是否删除了关键证据]
    D3 ~~~ D4[是否混入冲突材料] ~~~ D5[Token 截断发生在哪里]
  end
  subgraph GenerateCheck[检查生成]
    direction LR
    E1[模型是否遵守资料] --> E2[是否遗漏适用条件] --> E3[是否增加无来源事实]
    E3 --> E4[引用是否对应主张] --> E5[证据不足时是否承认未知]
  end
  subgraph Fix[根据故障位置修复]
    direction LR
    F1[补文件或修权限] ~~~ F2[更换解析与切块] ~~~ F3[调整召回和过滤]
    F3 ~~~ F4[调整重排与上下文] ~~~ F5[修改 Prompt 或模型]
  end
  SourceCheck --> ParseCheck
  ParseCheck --> RetrieveCheck
  RetrieveCheck --> RankCheck
  RankCheck --> GenerateCheck
  GenerateCheck --> Fix
  classDef source fill:#ECEFF1,stroke:#90A4AE,color:#263238
  classDef parse fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef retrieve fill:#FFF8E1,stroke:#FFD54F,color:#5D4037
  classDef rank fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  classDef generate fill:#E0F2F1,stroke:#4DB6AC,color:#004D40
  classDef fix fill:#E8F5E9,stroke:#81C784,color:#1B5E20
  class A1,A2,A3,A4,A5 source
  class B1,B2,B3,B4,B5 parse
  class C1,C2,C3,C4,C5 retrieve
  class D1,D2,D3,D4,D5 rank
  class E1,E2,E3,E4,E5 generate
  class F1,F2,F3,F4,F5 fix
```

这也是 RAG 评测需要分层的原因。

一套生产知识库需要保留每次查询的检索结果、过滤条件、重排分数、最终上下文、模型回答和引用。没有这些 Trace，开发者只会收到一句“它没找到明明存在的规定”，然后在切块大小、Embedding、Prompt 和模型之间盲目试错。

回到最开始的差旅问题。新政策经过解析、切块、Metadata 补充和索引进入知识库；用户提问以后，系统结合关键词、向量和过滤条件找到候选，再经过重排和上下文组装，把上海 600 元的条款连同生效日期交给模型。模型负责表达答案，外部资料负责提供依据。

RAG 没有让模型永久记住这些文件。它为每一次回答临时准备了一份可以回查的参考材料。

这套固定链路适合目标明确、一次检索能够找到主要证据的问题。遇到含糊提问、多跳关系或跨多个数据源的任务，一轮召回可能还不够。

## 参考资料

1. Patrick Lewis et al., [Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks](https://arxiv.org/abs/2005.11401).
2. OpenAI Developers, [Retrieval](https://developers.openai.com/api/docs/guides/retrieval).
