---
title: Deep Research 如何积累研究进展：从一次次尝试到一棵假设树
slug: deep-research-hypothesis-tree
date: 2026-07-15
priority: 5
allowCopy: true
description: 以 Arbor 的 Hypothesis Tree Refinement 为线索，讨论 Deep Research 如何保存假设、实验、失败经验与代码证据，让长期自主优化真正积累研究进展。
---

# Deep Research 如何积累研究进展：从一次次尝试到一棵假设树

一个 Agent 正在改进搜索系统。第一轮，它给查询生成器补了一段提示，让搜索词更具体；第二轮，它增加了查询数量；第三轮，它发现召回的网页太多，又在后面加了一层重排序；第四轮，它换了一套答案验证方法。

几十次实验跑下来，日志和代码分支越积越多。当前版本的分数可能比最初高，但有几件事已经说不清了：查询数量增加以后究竟带来了什么，重排序是在解决召回不足还是噪声太多，某个验证方案以前是否试过，以及下一轮为什么应该继续修改验证器。

Agent 一直在执行任务，研究却未必向前推进。一次实验得到的分数只对当前修改负责；如果实验背后的判断、失败原因和适用条件没有留下来，下一次尝试仍然要从一段冗长的历史里重新猜测。

[Arbor](https://arxiv.org/abs/2606.11926) 论文给出的 Hypothesis Tree Refinement，会把长期研究组织成一棵持续生长的假设树。树上保存的不只是接下来要做的实验，还有每个方向为什么值得尝试、实际发生了什么、结果支持或削弱了哪些判断，以及哪一个代码分支对应这份证据。

```mermaid
graph TB
  subgraph Attempt[普通的连续尝试]
    direction LR
    A1[修改提示词] --> A2[运行评测] --> A3[看到分数]
    A3 --> A4[继续改代码] --> A5[再次运行] --> A6[上下文逐渐堆满]
  end
  subgraph Missing[没有留下的研究状态]
    direction LR
    B1[修改对应什么判断] ~~~ B2[失败原因是什么] ~~~ B3[哪些条件已经被排除]
    B3 ~~~ B4[分数是否可以比较] ~~~ B5[后续方向受何影响]
  end
  subgraph Research[可以积累的研究过程]
    direction LR
    C1[提出可验证假设] --> C2[实现局部改动] --> C3[取得实验结果]
    C3 --> C4[提炼可复用洞察] --> C5[更新研究方向] --> C6[选择下一项实验]
  end
  subgraph Durable[持久化研究状态]
    direction LR
    D1[假设之间的关系] ~~~ D2[成功和失败证据] ~~~ D3[代码分支引用]
    D3 ~~~ D4[当前研究前沿] ~~~ D5[已经验证的最佳产物]
  end
  Attempt --> Missing
  Missing --> Research
  Research --> Durable
  classDef attempt fill:#FFF3E0,stroke:#FFB74D,color:#E65100
  classDef missing fill:#FFEBEE,stroke:#E57373,color:#B71C1C
  classDef research fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef durable fill:#E8F5E9,stroke:#81C784,color:#1B5E20
  class A1,A2,A3,A4,A5,A6 attempt
  class B1,B2,B3,B4,B5 missing
  class C1,C2,C3,C4,C5,C6 research
  class D1,D2,D3,D4,D5 durable
```

这和前面两篇文章讨论的网页型 Deep Research 不太一样。《Deep Research 是怎么工作的（上）》处理用户问题怎样变成研究任务，《Deep Research 是怎么工作的（下）》处理多路搜索结果怎样压缩、汇合并写成报告。Arbor 不以一份报告作为主要交付物。它面对的是一个能够反复修改和评测的研究产物，可能是一套模型训练代码、一个 Agent Harness，也可能是一条数据生成流水线。

## 研究对象被反复修改

Arbor 把自己的任务称为 Autonomous Optimization。系统拿到一份初始产物、一个自然语言目标和两套评测方式，在没有人逐步指定下一项实验的情况下，持续修改产物并验证效果。

例如，研究目标可以是提高 BrowseComp 上搜索 Agent 的回答准确率。初始产物是一套最小的 ReAct 搜索 Harness，Agent 可以修改查询生成、资料聚合、答案验证和上下文管理。每一次修改都能运行评测，所以研究过程会产生代码、分数和能够继续分析的错误案例。

网页型 Deep Research 的主要动作是搜索、阅读和综合。它从外部资料里收集证据，最后交付一份带引用的报告。Arbor 的主要动作是提出假设、修改代码和运行实验，最后交付改进后的产物，同时保留一棵完整的研究树。

```mermaid
graph TB
  subgraph WebInput[网页型 Deep Research 输入]
    direction LR
    A1[用户问题] ~~~ A2[Research Brief] ~~~ A3[来源要求] ~~~ A4[报告格式]
  end
  subgraph WebWork[网页型研究过程]
    direction LR
    B1[拆分子问题] --> B2[搜索和阅读] --> B3[提取资料证据]
    B3 --> B4[判断覆盖缺口] --> B5[统一写作] --> B6[绑定引用]
  end
  subgraph AOInput[自主实验研究输入]
    direction LR
    C1[初始代码或数据流程] ~~~ C2[优化目标] ~~~ C3[开发评测] ~~~ C4[保留测试]
  end
  subgraph AOWork[Arbor 研究过程]
    direction LR
    D1[提出研究假设] --> D2[修改研究产物] --> D3[运行开发实验]
    D3 --> D4[解释实验结果] --> D5[细化假设树] --> D6[验证候选分支]
  end
  subgraph Deliver[各自交付]
    direction LR
    E1[研究报告] ~~~ E2[来源与限定条件] ~~~ E3[改进后的产物] ~~~ E4[假设树与实验记录]
  end
  WebInput --> WebWork
  WebWork ~~~ AOInput
  AOInput --> AOWork
  AOWork --> Deliver
  classDef web fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef ao fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  classDef deliver fill:#E8F5E9,stroke:#81C784,color:#1B5E20
  class A1,A2,A3,A4,B1,B2,B3,B4,B5,B6 web
  class C1,C2,C3,C4,D1,D2,D3,D4,D5,D6 ao
  class E1,E2,E3,E4 deliver
```

这种区别决定了研究状态应该保存什么。网页研究需要把主张和资料来源对应起来；自主实验研究还要把假设、代码改动和评测结果对应起来。只保留最终代码，前面的失败会随分支一起被遗忘；只保留对话历史，Agent 又很难从几百次工具调用里恢复当前研究前沿。

Arbor 给出的任务接口里还有一个容易被忽略的条件：开发评测和保留测试使用不同证据。Agent 可以频繁查看开发评测，用它选择下一步；保留测试不能成为日常调参工具，只在判断一个候选方案是否应该进入当前最佳版本时使用。这样才能分清“适应了反复观察的开发反馈”和“得到可以迁移的真实改进”。

## 假设树和任务树

假设继续优化前面的搜索 Agent。任务列表里可能写着“修改查询生成器”“增加重排序”“改进答案验证”。这些任务能告诉 Executor 去哪里动手，却没有说明为什么要改，也没有给出实验以后如何判断方向的依据。

“给问题生成更多查询”仍然只是一个动作。更完整的假设应该说明：当前错误主要来自关键资料没有被召回；增加彼此不同的查询，可以扩大候选资料覆盖；如果判断成立，开发集上的证据召回和最终正确率应该一起提高。Executor 可以围绕这个判断实现实验，Coordinator 也能根据结果决定后续动作。

如果实验发现证据召回提高，回答准确率反而下降，这次尝试并非一无所获。它削弱了“召回不足是唯一问题”的判断，同时提供了一个新约束：后续方向要处理新增资料带来的噪声。于是树上可以继续长出“对资料做重排序”“按来源质量过滤”“先形成证据档案再回答”等更具体的子假设。

```mermaid
graph TB
  subgraph Goal[共同目标]
    direction LR
    A1[提高搜索 Agent 准确率] ~~~ A2[降低无依据回答] ~~~ A3[控制搜索成本]
  end
  subgraph TaskTree[任务树只描述动作]
    direction LR
    B1[修改查询生成] ~~~ B2[增加重排序] ~~~ B3[调整验证器] ~~~ B4[优化上下文]
  end
  subgraph Hypothesis[假设树描述可以检验的判断]
    direction LR
    C1[错误主要来自资料漏召回] --> C2[多样查询能够扩大证据覆盖]
    C2 --> C3[覆盖提高应带动正确率上升] --> C4[开发评测检查两个指标]
  end
  subgraph Evidence[实验返回的证据]
    direction LR
    D1[证据召回提高] ~~~ D2[回答准确率下降] ~~~ D3[上下文长度增加]
    D3 ~~~ D4[无关来源变多] ~~~ D5[原假设只得到部分支持]
  end
  subgraph Refine[证据推动细化]
    direction LR
    E1[增加资料重排序] ~~~ E2[建立来源质量过滤] ~~~ E3[先生成证据档案]
    E3 ~~~ E4[限制每类资料配额] ~~~ E5[保留漏召回方向继续观察]
  end
  Goal --> TaskTree
  TaskTree --> Hypothesis
  Hypothesis --> Evidence
  Evidence --> Refine
  classDef goal fill:#E8F5E9,stroke:#81C784,color:#1B5E20
  classDef task fill:#ECEFF1,stroke:#90A4AE,color:#263238
  classDef hypothesis fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef evidence fill:#FFF3E0,stroke:#FFB74D,color:#E65100
  classDef refine fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  class A1,A2,A3 goal
  class B1,B2,B3,B4 task
  class C1,C2,C3,C4 hypothesis
  class D1,D2,D3,D4,D5 evidence
  class E1,E2,E3,E4,E5 refine
```

论文把树根设为初始产物。靠近根部的节点是宽泛的研究方向，越往下才越接近可以执行的局部干预。一级节点可以是查询、资料聚合、答案验证三个方向；“增加查询数量”还不够具体，可以继续细化成“针对实体别名生成查询”“根据早期结果追问缺失条件”等实验。

树的边表示假设的细化、替代或修正，不表示实验发生的先后顺序。两个先后运行的实验可能属于不同方向，同一方向里的子节点也可能并行执行。这样一来，树展示的是研究判断怎样演变，而不是另一份换了形状的时间线。

## 节点要留下的信息

Arbor 的节点把三类内容放在一起：Hypothesis、Insight 和 Metadata。

Hypothesis 是实验开始前写下的判断。它应当可以被实现和评测，而且一旦交给 Executor 就不再改写。宽泛方向可以放在内部节点，真正派发的叶子节点则要具体到 Executor 知道该改哪里、预期观察什么。

Insight 是对证据的解释。叶子节点的 Insight 需要说清楚做了什么、发生了什么，以及结果怎样支持、削弱或限制原假设。内部节点的 Insight 会综合子节点，把局部结果压缩成这个方向目前已经知道的事情。

Metadata 负责把语义判断落回可检查的产物，包括节点状态、开发分数、事实结果、Git 分支引用和可选的背景证据。代码本身不复制进树里，节点只保存分支或提交引用。

```mermaid
graph TB
  subgraph Position[节点在树中的位置]
    direction LR
    A1[节点编号 1.2] ~~~ A2[父节点 查询策略] ~~~ A3[子节点 1.2.1 与 1.2.2]
    A3 ~~~ A4[深度决定假设粒度]
  end
  subgraph HypothesisField[Hypothesis]
    direction LR
    B1[当前问题判断] --> B2[准备采用的干预] --> B3[预期作用机制]
    B3 --> B4[可观察成功条件] --> B5[已知风险或冲突]
  end
  subgraph InsightField[Insight]
    direction LR
    C1[实际尝试内容] --> C2[实验发生的事实] --> C3[结果原因解释]
    C3 --> C4[支持或削弱程度] --> C5[后续可复用约束]
  end
  subgraph MetadataField[Metadata]
    direction LR
    D1[pending 或 running] ~~~ D2[done merged pruned] ~~~ D3[开发分数]
    D3 ~~~ D4[事实结果] ~~~ D5[代码分支引用] ~~~ D6[可选背景证据]
  end
  subgraph Persist[持久化与查看]
    direction LR
    E1[JSON 作为权威状态] --> E2[每次变更立即保存] --> E3[Markdown 供人检查]
    E3 --> E4[上下文压缩后重新读取] --> E5[崩溃或重启后恢复]
  end
  Position --> HypothesisField
  HypothesisField --> InsightField
  InsightField --> MetadataField
  MetadataField --> Persist
  classDef position fill:#ECEFF1,stroke:#90A4AE,color:#263238
  classDef hypothesis fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef insight fill:#FFF8E1,stroke:#FFD54F,color:#5D4037
  classDef metadata fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  classDef persist fill:#E8F5E9,stroke:#81C784,color:#1B5E20
  class A1,A2,A3,A4 position
  class B1,B2,B3,B4,B5 hypothesis
  class C1,C2,C3,C4,C5 insight
  class D1,D2,D3,D4,D5,D6 metadata
  class E1,E2,E3,E4,E5 persist
```

节点会经历 `pending`、`running`、`done`，之后可能成为 `merged` 或 `pruned`。这里的 `pruned` 不能简单理解成“删除”。节点仍然保存在树中，它只是退出当前待执行前沿。下一轮提出新假设时，Coordinator 会读取这些失败经验，避免换个说法再跑一遍同样的方向。

因此，这棵树同时做三份工作。待执行的叶子节点组成当前搜索前沿；已经完成的节点构成长程记忆；假设、证据和代码引用之间的联系又形成一份可审计记录。研究跑得越久，这种一物多用越重要。

```mermaid
graph TB
  subgraph Root[根节点 初始搜索 Harness]
    direction LR
    A1[初始实现] ~~~ A2[基线开发分数] ~~~ A3[基线测试分数] ~~~ A4[研究目标和评测命令]
  end
  subgraph Directions[一级研究方向]
    direction LR
    B1[查询与探索策略] ~~~ B2[资料聚合与上下文] ~~~ B3[答案生成与验证]
  end
  subgraph QueryBranch[查询方向的实验]
    direction LR
    C1[1.1 多样查询 已完成] ~~~ C2[1.2 实体别名查询 已合并] ~~~ C3[1.3 固定增加轮次 已剪枝]
    C3 ~~~ C4[1.4 缺口驱动追问 待执行]
  end
  subgraph EvidenceBranch[资料方向的实验]
    direction LR
    D1[2.1 来源重排序 已完成] ~~~ D2[2.2 证据档案 已合并] ~~~ D3[2.3 全文全部注入 已剪枝]
    D3 ~~~ D4[2.4 冲突证据分组 待执行] ~~~ D5[2.5 来源配额 待执行]
  end
  subgraph VerifyBranch[验证方向的实验]
    direction LR
    E1[3.1 回答后自检 已完成] ~~~ E2[3.2 引用支持检查 正在运行]
    E2 ~~~ E3[3.3 多数投票 已剪枝] ~~~ E4[3.4 失败类型专用验证 待执行]
  end
  subgraph TreeRoles[整棵树提供的能力]
    direction LR
    F1[搜索前沿] ~~~ F2[成功与失败记忆] ~~~ F3[方向级洞察]
    F3 ~~~ F4[代码和证据追溯] ~~~ F5[当前最佳产物]
  end
  Root --> Directions
  Directions --> QueryBranch
  QueryBranch ~~~ EvidenceBranch
  EvidenceBranch ~~~ VerifyBranch
  VerifyBranch --> TreeRoles
  classDef root fill:#E8F5E9,stroke:#81C784,color:#1B5E20
  classDef direction fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef query fill:#FFF8E1,stroke:#FFD54F,color:#5D4037
  classDef evidence fill:#E0F2F1,stroke:#4DB6AC,color:#004D40
  classDef verify fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  classDef role fill:#ECEFF1,stroke:#90A4AE,color:#263238
  class A1,A2,A3,A4 root
  class B1,B2,B3 direction
  class C1,C2,C3,C4 query
  class D1,D2,D3,D4,D5 evidence
  class E1,E2,E3,E4 verify
  class F1,F2,F3,F4,F5 role
```

上图是说明结构而构造的搜索 Harness 的示例，不是论文里某次实验的原样复刻。真正的树也不需要每个方向长出同样数量的节点。证据丰富、仍有潜力的方向可以继续展开；一个关键前提已经被否定的方向，可能只运行一次就停止。树的形状应该跟着研究结果走，不必整整齐齐。

## 实验怎样改变树

有了数据结构，Arbor 还需要一套让树持续更新的循环。论文把 Coordinator 的工作归纳为 Observe、Ideate、Select、Dispatch、Backpropagate 和 Decide。

Observe 不是简单回看上一轮消息。Coordinator 会重新读取持久化的树，找出待执行叶子、最近返回的结果、祖先节点里的洞察和当前最佳产物。即使聊天历史经过压缩，它也可以从树中恢复“哪些方向有效、哪些已经排除、哪里仍有矛盾”。

Ideate 在已有证据下面提出一小组子假设。得到验证的洞察可以成为下一步的前提，失败节点则成为负面约束。如果“无差别增加网页数量”已经导致噪声上升，新的节点就不应再次提出同一种干预，而应处理资料筛选、分组或冲突判断。

Select 决定下一批值得执行的叶子。它并非只挑预期分数最高的节点。有些实验即使失败，也能澄清一个承重假设；有些方向和刚完成的兄弟节点可以形成对照；还有些实验收益可能很高，但实现成本已经超过剩余预算。选择本身就是对研究前沿的管理。

Dispatch 把固定假设交给独立 Executor。Backpropagate 收回开发分数、事实结果、洞察和代码分支，把叶子结果写入对应节点，再沿祖先路径向上综合。Decide 最后判断继续展开、剪枝、停止，还是把候选分支送去保留测试。

```mermaid
graph TB
  subgraph Observe[Observe 重新读取研究状态]
    direction LR
    A1[查看当前树形] --> A2[读取待执行叶子] --> A3[检查最近实验]
    A3 --> A4[加载祖先洞察] --> A5[确认当前最佳产物] --> A6[整理有效和失败约束]
  end
  subgraph Ideate[Ideate 生成证据约束下的假设]
    direction LR
    B1[选择需要细化的父节点] --> B2[复用已验证前提] --> B3[避开已剪枝模式]
    B3 --> B4[提出机制和干预] --> B5[写明成功条件] --> B6[加入待执行节点]
  end
  subgraph Select[Select 管理当前研究前沿]
    direction LR
    C1[比较预期收益] ~~~ C2[考虑实现成本] ~~~ C3[检查方向多样性]
    C3 ~~~ C4[优先消除关键不确定性] ~~~ C5[选择可并行叶子]
  end
  subgraph Dispatch[Dispatch 独立执行]
    direction LR
    D1[固定假设] --> D2[附带祖先洞察] --> D3[创建隔离 worktree]
    D3 --> D4[实现局部修改] --> D5[运行开发评测] --> D6[形成结构化报告]
  end
  subgraph Backpropagate[Backpropagate 回写和抽象]
    direction LR
    E1[写回分数] ~~~ E2[写回事实结果] ~~~ E3[写回代码引用]
    E3 ~~~ E4[提炼叶子洞察] ~~~ E5[更新方向级认识] ~~~ E6[更新根节点全局认识]
  end
  subgraph Decide[Decide 决定下一轮]
    direction LR
    F1[继续细化] ~~~ F2[切换方向] ~~~ F3[剪枝并保留证据]
    F3 ~~~ F4[尝试保留测试] ~~~ F5[合并候选] ~~~ F6[预算耗尽后停止]
  end
  Observe --> Ideate
  Ideate --> Select
  Select --> Dispatch
  Dispatch --> Backpropagate
  Backpropagate --> Decide
  Decide -.-> Observe
  classDef observe fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef ideate fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  classDef select fill:#FFF8E1,stroke:#FFD54F,color:#5D4037
  classDef dispatch fill:#E0F2F1,stroke:#4DB6AC,color:#004D40
  classDef back fill:#FFF3E0,stroke:#FFB74D,color:#E65100
  classDef decide fill:#E8F5E9,stroke:#81C784,color:#1B5E20
  class A1,A2,A3,A4,A5,A6 observe
  class B1,B2,B3,B4,B5,B6 ideate
  class C1,C2,C3,C4,C5 select
  class D1,D2,D3,D4,D5,D6 dispatch
  class E1,E2,E3,E4,E5,E6 back
  class F1,F2,F3,F4,F5,F6 decide
```

这里的 Backpropagate 和树搜索里的数值回传不完全一样。Arbor 向上传递的不只有一个分数，还包括失败归因、适用条件和能够复用的经验。一个叶子发现“新加入的网页没有进入答案生成所使用的代码路径”，传到上层以后，可以形成“先验证干预是否真正作用于目标路径”的方向级约束。后续兄弟节点都会从中受益。

这也解释了为什么失败节点不能随手删掉。失败至少有三种情况：假设本身不成立，实现存在错误，或者评测没有真正覆盖修改路径。三者对下一步的影响完全不同。只有保存事实结果和解释，Coordinator 才知道该剪掉研究方向、让 Executor 修复实现，还是补一项更合适的评测。

## 方向，实验

长程研究里的全局判断和一次实验所需的工程工作，使用的是两种不同视野。

Coordinator 能看到整棵树，负责比较方向、控制预算、传播洞察和决定是否合并。它保持长期运行，却不直接进入每个分支改代码。Executor 生命周期较短，只接收一个叶子假设、相关祖先洞察、当前最佳产物和开发评测命令。它在独立 Git worktree 里完成修改、调试和评测，然后返回结构化结果。

Executor 可以修复自己的实现错误。例如评测脚本没有跑通，或者改动没有进入实际调用路径，它可以继续检查和重跑。它不能因为分数不好就把“增加来源质量过滤”悄悄换成“重写答案验证器”。如果执行时连假设也一起换掉，最后返回的分数就无法说明原节点是否成立，树里的因果归属也会变得含糊不清。

```mermaid
graph TB
  subgraph Contract[任务合同和持久化状态]
    direction LR
    A1[初始产物] ~~~ A2[研究目标] ~~~ A3[开发评测命令]
    A3 ~~~ A4[保留测试命令] ~~~ A5[假设树 JSON] ~~~ A6[当前最佳主线]
  end
  subgraph Coordinator[长期 Coordinator]
    direction LR
    B1[读取全局研究前沿] --> B2[提出和选择假设] --> B3[控制并行与预算]
    B3 --> B4[派发固定假设] --> B5[整合返回证据] --> B6[剪枝或申请合并]
  end
  subgraph Executors[短期 Executors]
    direction LR
    C1[Executor A 查询策略] ~~~ C2[Executor B 证据档案] ~~~ C3[Executor C 引用验证]
    C3 ~~~ C4[每个实例只负责一个叶子]
  end
  subgraph Worktrees[相互隔离的实验环境]
    direction LR
    D1[worktree A 修改和评测] ~~~ D2[worktree B 修改和评测] ~~~ D3[worktree C 修改和评测]
    D3 ~~~ D4[全部从当前最佳版本创建] ~~~ D5[互不污染共享主线]
  end
  subgraph Reports[统一返回合同]
    direction LR
    E1[开发分数] ~~~ E2[事实结果] ~~~ E3[提炼洞察]
    E3 ~~~ E4[分支引用] ~~~ E5[实现选择与异常说明]
  end
  subgraph Boundaries[职责限制]
    direction LR
    F1[Coordinator 不直接改目标代码] ~~~ F2[Executor 不修改共享树]
    F2 ~~~ F3[Executor 不改已分配假设] ~~~ F4[Executor 不使用保留测试调参]
  end
  Contract --> Coordinator
  Coordinator --> Executors
  Executors --> Worktrees
  Worktrees --> Reports
  Reports --> Boundaries
  classDef contract fill:#ECEFF1,stroke:#90A4AE,color:#263238
  classDef coordinator fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef executor fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  classDef worktree fill:#E0F2F1,stroke:#4DB6AC,color:#004D40
  classDef report fill:#FFF8E1,stroke:#FFD54F,color:#5D4037
  classDef boundary fill:#FFEBEE,stroke:#E57373,color:#B71C1C
  class A1,A2,A3,A4,A5,A6 contract
  class B1,B2,B3,B4,B5,B6 coordinator
  class C1,C2,C3,C4 executor
  class D1,D2,D3,D4,D5 worktree
  class E1,E2,E3,E4,E5 report
  class F1,F2,F3,F4 boundary
```

独立 worktree 也不只是为了并行。它让每份实验结果都有一个清楚的产物引用。未通过验证的修改留在自己的分支，不会污染当前最佳版本；需要比较兄弟节点时，几个 Executor 又可以从同一个起点出发，减少基础版本不同带来的干扰。

这种分工和常见的多 Agent 流程有一点差异。多个角色并不自动产生可靠研究，关键在于每个角色能看到什么、能修改什么，以及返回内容怎样进入持久状态。Arbor 用很窄的交接合同维持这层关系：Coordinator 派下去的是固定假设，Executor 交回来的是分数、事实、洞察和分支引用。

## 不能直接合并

如果 Agent 每轮都查看同一批开发问题，它迟早会摸到这批问题的脾气。某项修改可能只适合开发集，也可能钻了评测器的空子。继续用开发分数决定最终版本，容易把局部适配误当成研究进展。

Arbor 让开发评测负责探索。Executor 可以反复使用它检查修改是否有效，Coordinator 也依据开发结果选择和细化方向。候选分支要进入当前最佳主线，还必须在新的 worktree 中经过保留测试，并且确实优于当前最佳版本。保留测试只负责准入，不向普通 Executor 开放，也不能被拿来日常调参。

```mermaid
graph TB
  subgraph Baseline[当前最佳产物]
    direction LR
    A1[主线代码] ~~~ A2[已记录开发分数] ~~~ A3[已记录保留测试分数] ~~~ A4[已合并假设路径]
  end
  subgraph Explore[开发评测负责探索]
    direction LR
    B1[创建实验分支] --> B2[实现固定假设] --> B3[运行开发评测]
    B3 --> B4[修复实现问题] --> B5[记录事实和洞察] --> B6[与兄弟节点比较]
  end
  subgraph Candidate[选择候选分支]
    direction LR
    C1[开发分数有改善] ~~~ C2[修改路径确实生效] ~~~ C3[结果能够归因]
    C3 ~~~ C4[没有违反研究合同] ~~~ C5[进入准入检查]
  end
  subgraph HeldOut[保留测试负责准入]
    direction LR
    D1[在新 worktree 验证] --> D2[运行不可用于调参的测试] --> D3[对比当前最佳]
    D3 --> D4[检查改善是否迁移] --> D5[形成通过或拒绝结果]
  end
  subgraph Promote[通过后的处理]
    direction LR
    E1[合并候选分支] --> E2[更新当前最佳产物] --> E3[节点标记 merged]
    E3 --> E4[保存新基线] --> E5[后续实验从新主线出发]
  end
  subgraph Reject[未通过后的处理]
    direction LR
    F1[不合并代码] --> F2[节点保留为 done 或 pruned] --> F3[记录开发和测试分歧]
    F3 --> F4[提炼过拟合线索] --> F5[约束下一轮假设]
  end
  Baseline --> Explore
  Explore --> Candidate
  Candidate --> HeldOut
  HeldOut --> Promote
  Promote ~~~ Reject
  classDef baseline fill:#ECEFF1,stroke:#90A4AE,color:#263238
  classDef explore fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef candidate fill:#FFF8E1,stroke:#FFD54F,color:#5D4037
  classDef heldout fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  classDef promote fill:#E8F5E9,stroke:#81C784,color:#1B5E20
  classDef reject fill:#FFEBEE,stroke:#E57373,color:#B71C1C
  class A1,A2,A3,A4 baseline
  class B1,B2,B3,B4,B5,B6 explore
  class C1,C2,C3,C4,C5 candidate
  class D1,D2,D3,D4,D5 heldout
  class E1,E2,E3,E4,E5 promote
  class F1,F2,F3,F4,F5 reject
```

论文在 Terminal-Bench 2.0 上给出了一个很直观的例子。Claude Code 的开发通过率达到 `75.00`，高于 Arbor 的 `72.22`；到了保留测试，Claude Code 得到 `71.70`，Arbor 则达到 `77.36`。只看开发分数会选错候选，这正是 Merge Gate 要防住的事情。

测试没有通过的分支也不是白跑。开发集表现好、保留测试表现差，本身就是一份有价值的证据：当前方向可能在利用开发集特征，而没有产生可迁移改进。它会留在树里，影响下一轮选点和假设生成。

## 网页型 Deep Research

网页型 Deep Research 不一定要照搬 Arbor 的 Git worktree 和开发评测，因为它处理的主要产物是一组研究结论。不过，假设树对研究状态的组织方式仍然有参考价值。

网页研究也会遇到相互竞争的解释。例如研究“某个 Agent SDK 是否支持长期恢复”时，一个方向认为它提供原生工作流持久化，另一个方向认为它只保存对话历史。Researcher 可以分别寻找接口文档、代码实现和工程案例。树上的叶子不再绑定代码实验，而是绑定一项资料验证；事实结果保存找到的原文，Insight 记录证据支持哪种解释、还缺什么条件。

开发评测和保留测试也不必机械映射。网页研究可以借用的是“探索反馈与最终准入分开”这层关系：搜索摘要和单个网页可以帮助继续追查，但一个结论进入最终报告以前，还要检查来源质量、交叉验证、时间有效性和引用是否真的支持附近的主张。

```mermaid
graph TB
  subgraph ArborState[Arbor 中的研究状态]
    direction LR
    A1[可验证代码假设] ~~~ A2[独立实验分支] ~~~ A3[开发评测结果]
    A3 ~~~ A4[保留测试] ~~~ A5[合并后的最佳产物]
  end
  subgraph WebState[网页研究中的对应做法]
    direction LR
    B1[待验证解释或主张] ~~~ B2[独立资料搜索方向] ~~~ B3[找到的原始证据]
    B3 ~~~ B4[交叉验证和引用检查] ~~~ B5[进入最终报告的结论]
  end
  subgraph Reusable[可以直接借用的原则]
    direction LR
    C1[宽方向逐步细化] --> C2[成功失败都留下证据] --> C3[局部发现向上压缩]
    C3 --> C4[下一轮读取已有约束] --> C5[研究过程可恢复] --> C6[最终判断可以回查]
  end
  subgraph Avoid[不能直接照搬的部分]
    direction LR
    D1[网页研究未必有可运行代码] ~~~ D2[来源证据没有统一标量分数]
    D2 ~~~ D3[不同观点不一定互相排斥] ~~~ D4[报告结论常带条件和不确定性]
  end
  subgraph Result[得到的研究过程]
    direction LR
    E1[问题拆解会随证据更新] ~~~ E2[失败查询不会被重复]
    E2 ~~~ E3[冲突资料成为新研究方向] ~~~ E4[结论保留来源和限定条件]
  end
  ArborState ~~~ WebState
  WebState --> Reusable
  Reusable --> Avoid
  Avoid --> Result
  classDef arbor fill:#F3E5F5,stroke:#BA68C8,color:#4A148C
  classDef web fill:#E3F2FD,stroke:#64B5F6,color:#0D47A1
  classDef reusable fill:#E8F5E9,stroke:#81C784,color:#1B5E20
  classDef avoid fill:#FFF3E0,stroke:#FFB74D,color:#E65100
  classDef result fill:#E0F2F1,stroke:#4DB6AC,color:#004D40
  class A1,A2,A3,A4,A5 arbor
  class B1,B2,B3,B4,B5 web
  class C1,C2,C3,C4,C5,C6 reusable
  class D1,D2,D3,D4 avoid
  class E1,E2,E3,E4 result
```

前面的 Deep Research 文章已经讨论过 Research Brief、并行 Researcher、结果压缩和统一写作。假设树补上的，是这些步骤之间长期存在的研究状态。计划不再只是一份开工前生成的目录，它会被实验和证据持续修改；记忆也不再等同于聊天记录，而是能够参与下一轮选点的假设、结果和洞察。

把分散的尝试组织成有来有往的研究过程，让成功能够继续展开，让失败能够约束下一步，也让最终产物找得到一路走来的证据。

## 参考资料

1. Jiajie Jin et al., [Toward Generalist Autonomous Research via Hypothesis-Tree Refinement](https://arxiv.org/abs/2606.11926).
2. RUC-NLPIR, [Arbor](https://github.com/RUC-NLPIR/Arbor).
