---
title: 并发编程体系
priority: 3
slug: concurrency
date: 2026-01-30
description: 包含 Java 的线程与锁机制（Thread, Synchronized, ReentrantLock, JUC）、Kotlin 的协程（Coroutines, Flow）、以及 Android 特有的线程模型（Handler, AsyncTask, HandlerThread）。
---

包含 Java 的线程与锁机制（Thread, Synchronized, ReentrantLock, JUC）、Kotlin 的协程（Coroutines, Flow）、以及 Android 特有的线程模型（Handler, AsyncTask, HandlerThread）。